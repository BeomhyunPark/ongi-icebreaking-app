# 온기 앱 확장 리팩터링 계획

> 아래는 초기 앱 확장 당시의 작업 기록입니다. 현재 구조와 수정 기준은 [코드 구조 가이드](docs/ARCHITECTURE.md)를 따릅니다. 아래의 문항 수·테스트 수·예정 콘텐츠는 당시 기준이며 현재 완료 기준이 아닙니다.

## 목표

새 놀거리를 추가할 때 기존 `마음속 흔적 찾기`의 상태, 데이터, 화면을 수정하지 않아도 되도록 앱 셸과 기능 경계를 분리한다. 사용자 동작과 결과 계산은 현재 테스트 계약을 유지한다.

## 구조 원칙

- `src/app`: 앱 진입, 스플래시, 활동 카탈로그와 실행 registry만 담당한다.
- `src/features/<activity>`: 활동별 데이터, 도메인, 상태, 화면, 브라우저 서비스를 소유한다.
- `src/components`: 둘 이상의 기능에서 재사용하는 UI만 둔다.
- 기능 내부 코드는 다른 기능 내부 코드를 직접 import하지 않는다.
- 공통화는 두 기능에서 실제 중복이 확인된 뒤 수행한다.

## 진행 상태

### 완료 — 앱 셸과 기능 경계

- 홈/스플래시와 활동 실행 흐름 분리
- 활동 ID와 실행 컴포넌트를 연결하는 registry 추가
- `heart-trace`의 데이터, 도메인, 상태, 화면, 결과 이미지 서비스를 기능 폴더로 이동
- 홈 화면과 홈 전용 설치/공유 UI를 `features/home`으로 이동
- 기존 `App` 공개 진입점과 사용자 흐름 유지

### 완료 — 스타일 경계

- 단일 `src/styles/app.css`를 공통, 홈, 마음의 흔적 스타일로 분리
- 기존 선택자와 선언 순서를 유지한 채 1차 분리
- 분리 전후 CSS 원문과 프로덕션 CSS 번들 해시가 동일한지 검증

스타일 파일은 원래 cascade 순서대로 `main.tsx`에서 import한다. `responsive.css`에는 여러 화면에 걸친 미디어 쿼리를 모아 두어, 화면별 파일이 서로를 import하지 않도록 한다.

### 완료 — 플랫폼 경계

- PWA 등록을 앱 진입점에서 `platform` 서비스로 분리
- 설치 프롬프트와 공유 기능의 브라우저 API를 테스트 가능한 서비스로 이동
- 결과 이미지 준비/저장 상태를 화면 컴포넌트에서 전용 hook으로 분리

서비스 워커 캐시 버전은 배포 단위에서 변경한다. 캐시 전략 자체를 바꿀 때는 오프라인 재접속과 새 버전 갱신 시나리오를 별도로 검증한다.

### 시범판 완료 — 첫 신규 활동

- `balance-game` 가벼운 질문 8개와 기능 흐름 구현
- 활동 registry에 lazy loading을 적용해 기능별 JavaScript 번들 분리
- 추천 5문항 흐름과 리더 직접 선택 방식 제공
- 대화 온도 선택에서 `가볍게`를 제공하고 `조금 깊게`는 후속 콘텐츠로 분리
- 카탈로그 활성화 및 registry 등록
- 활동별 직접 URL이 필요하면 라우터와 정적 호스팅 fallback을 함께 도입

## 단계별 완료 기준

모든 구조 변경은 다음 조건을 만족해야 한다.

- `npm run verify` 통과
- 기존 95개 동작/도메인 테스트 유지
- 결과 유형과 점수 계산 결과 불변
- 홈 → 검사 → 결과 → 다시 하기/홈 이동 흐름 불변
- 320px 이상 모바일 레이아웃과 키보드 접근성 회귀 없음
