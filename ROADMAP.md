# 온기 제품 로드맵

> 놀거리는 처음 방문하게 만들고, 공동체 도구는 다시 찾게 만들며, 함께 참여하는 경험은 서로의 대화를 시작하게 한다.

- 마지막 정리: 2026-09-03
- 현재 배포 버전: **v2.2.1**
- 다음 정식 배포 후보: **v2.3.0 · 구르미가 찾아오는 날**
- 버전은 목표의 순서를 뜻하며 출시 날짜를 약속하지 않는다.

## 1. 온기가 만들고 싶은 것

온기는 단순한 미니게임 모음이 아니라 **교회 공동체가 어색함을 덜고, 서로를 알아가고, 모임을 편하게 진행하도록 돕는 모바일 서비스**다.

제품은 세 가지 경험을 함께 키운다.

1. **발견하기**
   - 성격 테스트, 밸런스 게임, 월드컵처럼 나와 서로의 취향을 발견한다.
2. **진행하기**
   - 순서 정하기, 조 편성, 기도 후원처럼 리더가 모임을 매끄럽게 진행한다.
3. **연결하기**
   - QR을 통해 각자의 휴대폰이 같은 모임에 참여하고 이야기를 나눈다.

새 기능의 수보다 “실제 모임에서 설명 없이 쓸 수 있는가”를 더 중요한 기준으로 삼는다.

## 2. 제품 원칙

- **모바일 우선:** 320px 이상의 작은 화면과 한 손 조작을 기본으로 설계한다.
- **가입 없이 시작:** 개인 계정이 꼭 필요한 이유가 생기기 전까지 QR과 링크로 바로 참여한다.
- **Local-first와 Server-assisted의 구분:** 한 기기에서 끝나는 도구는 브라우저에, 여러 기기가 함께 쓰는 상태만 서버에 둔다.
- **개인정보 최소 수집:** 이름과 답변은 기능 수행에 필요한 동안만 사용하고 삭제 시점을 화면에 알린다.
- **내용을 분석 데이터로 남기지 않기:** 문항별 답변과 익명 나눔 원문은 제품 통계 목적으로 저장하지 않는다.
- **실패해도 핵심 기능 유지:** API나 홈서버에 문제가 생겨도 기존 테스트와 단일 기기 도구는 계속 사용할 수 있어야 한다.
- **공유되는 순간까지 제품:** 링크 제목, 설명, 미리보기 이미지와 결과 카드도 본 화면과 같은 품질로 관리한다.
- **접근성:** 키보드, 화면 읽기, 충분한 터치 영역, 저속 모션과 색상 대비를 기능 완료 기준에 포함한다.
- **한 릴리스에 한 가지 중심 변화:** 큰 목표 하나와 그 목표를 완성하는 범위만 묶는다.

## 3. 지금까지 만들어진 기반

### 사용자 경험

- 마음속 흔적 찾기 성격 테스트와 다섯 결과 유형
- 가볍게·조금 깊게로 나뉜 극과 극 밸런스 게임
- 음식, 여행지, 무료 이용권 등 여섯 주제의 최애 월드컵
- 사다리, 제비뽑기, 기도·나눔 순서, 조 편성, 원투원, 기도 후원을 제공하는 오늘은 누구?
- QR로 함께 참여하는 익명으로 만나는 우리
- 두 번째 기질 테스트 구르미 테스트 티저
- PWA 설치, 활동별 직접 링크, 결과 이미지 저장·공유
- 콘텐츠와 세부 카테고리별 좋아요, 홈 누적 방문자
- 앱 안에서 확인하는 업데이트 내역

### 서버와 운영

- Spring Boot API, PostgreSQL, Flyway migration
- HttpOnly session cookie와 hash token을 사용하는 무가입 Room
- SSE 기반 참여 현황과 나눔 진행 상태 동기화
- 중복 요청, 오래된 화면의 조작과 Room 간 데이터 혼합 방지
- Room 종료 시 참여자 이름과 답변 즉시 삭제
- 익명 조회·참여·완료·좋아요·공유 집계
- GitHub Pages frontend 자동 배포
- Surface Pro 홈서버 Backend·DB와 Cloudflare Tunnel
- Backend 변경 감지, 검증, 자동 배포와 readiness 실패 시 직전 image 복구
- 단계별 부하 테스트와 Docker resource 지표 수집 도구

### 운영 과제

- iOS Safari와 Android Chrome 실제 기기 회귀 테스트의 정례화
- 장시간 SSE 연결, background 복귀와 Wi-Fi·모바일 데이터 전환 검증
- 홈서버 재부팅·정전·Tunnel 단절 이후 자동 복구 훈련
- 외부 backup 보관과 실제 restore 훈련
- Open Graph cache가 오래 남는 플랫폼별 공유 이미지 검증
- 익명 통계의 중복·누락 여부를 운영 데이터로 확인하는 절차
- 현재 상태와 맞지 않는 문서·버전·카피의 지속적인 정리

## 4. 릴리스 지도

| 버전 후보 | 한 줄 목표 | 상태 |
|---|---|---|
| **v2.2.x** | 현재 기능과 홈서버 운영을 안정화 | 상시 유지보수 |
| **v2.3.0** | 두 번째 기질 테스트 구르미 테스트 정식 공개 | **다음** |
| **v2.3.x** | 구르미 결과·공유·모바일 호환성 안정화 | 후속 |
| **v2.4.0** | 각자의 휴대폰으로 함께 하는 실시간 밸런스 | 계획 |
| **v2.5.0** | 여러 도구를 하나의 모임 진행 코스로 연결 | 계획 |
| **v2.6.0** | 시즌과 모임에 맞는 콘텐츠 팩 제공 | 후보 |
| **v2.7.0** | 익명 나눔을 질문함·기도제목까지 확장 | 후보 |
| **v2.8.0** | 개인정보 없이 서비스 상태를 보는 운영 화면 | 후보 |
| **v3.0.0** | 반복 사용이 확인된 공동체를 위한 공간과 권한 | 조건부 |

긴급 버그, 보안 수정, 잘못된 결과나 공유 이미지 수정은 다음 기능 버전을 기다리지 않고 patch 버전으로 먼저 배포한다.

## 5. 상세 릴리스 후보

### v2.2.x — 지금의 온기를 단단하게

새 기능보다 현재 공개 기능과 홈서버의 신뢰도를 높이는 상시 유지보수 구간이다.

주요 범위:

- 실제 모바일에서 테스트 시작부터 결과 공유까지 회귀 검증
- 익명 나눔의 생성·입장·작성·공개·종료 전 구간 rehearsal
- 방문, 참여 시작, 완료, 좋아요와 공유 집계의 중복·누락 점검
- 느린 네트워크와 API 오류에서 기능별 실패 안내 정리
- PWA update와 offline fallback 점검
- 공유 플랫폼별 Open Graph 이미지 cache 갱신 확인
- 홈서버 backup·restore와 자동 rollback 훈련

완료 기준:

- 공개된 기존 기능의 치명적 오류가 없다.
- 7명이 한 Room의 전체 흐름을 반복해서 완료할 수 있다.
- 서버가 끊겨도 정적 놀거리와 단일 기기 도구는 계속 열린다.
- 배포 실패 시 공개 서비스와 DB가 이전 정상 상태를 유지한다.

---

### v2.3.0 — 구르미가 찾아오는 날

흔적 테스트를 잇는 두 번째 기질 테스트 구르미 테스트를 정식 공개한다. 이번 버전의 핵심은 캐릭터를 많이 보여주는 것이 아니라, **질문에서 결과까지 납득되고 다시 나누고 싶은 테스트**를 만드는 것이다.

#### 콘텐츠와 판정 모델

- 여덟 캐릭터의 핵심 기질, 강점, 어려움과 관계 속 모습을 하나의 문서로 확정
- 질문 ID와 선택지별 점수 방향을 고정하고 중복·편향 검토
- 동일 응답은 항상 동일 결과가 나오는 결정적 scoring
- 동점, 근소한 점수 차와 미응답을 처리하는 명확한 정책
- 질문과 결과 문구를 content version으로 관리
- 공개 전 실제 사용자 소규모 검토와 문구 수정 기록

#### 테스트 경험

- 테스트의 차이를 한눈에 알리는 인트로
- 현재 위치와 남은 분량이 분명한 질문 진행
- 실수한 선택을 되돌릴 수 있는 이전 문항 이동
- 중복 탭이나 빠른 연속 입력에도 문항이 건너뛰지 않는 상태 관리
- 결과가 만들어지는 시간을 지루하지 않게 잇는 구르미 모션
- 처음부터 다시 하기와 홈으로 돌아가기
- 320px 화면, 작은 높이의 모바일과 safe area 대응

#### 여덟 결과

- 캐릭터별 대표 이미지와 한 문장 요약
- 기본 기질, 관계에서 보이는 모습, 강점과 조심할 점
- 공동체 안에서 서로를 이해하는 데 도움이 되는 대화 질문
- 결과를 우열이나 낙인으로 읽지 않도록 안내
- 모든 결과의 분량, 말투와 시각적 위계를 같은 기준으로 검수

#### 저장과 공유

- 여덟 결과별 저장·공유용 이미지
- 결과별 제목, 설명과 Open Graph 미리보기
- 앱 직접 링크와 공유 중계 페이지
- Android, iOS Safari와 카카오톡 인앱 브라우저에서 저장·공유 fallback
- 오래된 티저 링크가 정식 테스트로 자연스럽게 이어지는 전환

#### 익명 통계와 출시

- 구르미를 DRAFT에서 PUBLISHED content version으로 전환
- 조회, 참여 시작, 문항 도달, 완료, 결과 분포와 만족도를 익명 attempt 기준으로 집계
- Beta 문항 검증을 위해 version에 묶인 raw answer와 응답 시간을 저장하되 개인정보와 raw IP는 추가 수집하지 않음
- 홈 티저를 정식 카드로 교체하고 업데이트 내역 추가
- PWA cache와 이전 버전 브라우저의 asset 갱신 확인
- 문제 발생 시 티저 또는 이전 안정 버전으로 되돌릴 수 있는 배포 절차

이번 버전에서 제외:

- 궁합 점수와 친구 비교
- 로그인과 결과 영구 보관
- AI가 개인 성향을 추가 해석하는 기능
- 질문과 결과를 운영 화면에서 실시간 편집하는 CMS

완료 기준:

- 여덟 결과가 모두 실제 이미지·문구·공유 카드까지 완성된다.
- scoring과 동점 정책이 자동 테스트로 고정된다.
- 질문 순서와 선택지 연결이 바뀌어도 잘못된 결과가 나오지 않는다.
- 대표 모바일 환경에서 시작부터 결과 공유까지 막힘이 없다.
- 접근성 자동 검사와 키보드 흐름이 통과한다.
- 참여 시작·완료·결과 통계가 중복 없이 기록된다.

#### v2.3.x — 구르미 출시 후 안정화

- 결과별 완료율과 중도 이탈 지점을 익명 집계로 확인
- 오해하기 쉬운 문구와 지나치게 긴 결과 설명 다듬기
- 특정 결과로 과도하게 쏠리는지 scoring 분포 검토
- 결과 카드를 저장하지 못하는 기기와 공유 앱 호환성 수정
- 점수 규칙을 바꿀 때 기존 결과와 섞이지 않도록 새 content version 발행

표본이 적을 때 결과 분포만 보고 scoring을 즉시 바꾸지 않는다. 사용자 인터뷰와 문항 검토가 함께 있을 때만 판정 모델을 조정한다.

---

### v2.4.0 — 같이 고르는 실시간 밸런스

익명 나눔에서 검증한 Room과 SSE 기반을 기존 놀거리에 처음 재사용한다.

사용 흐름:

1. 진행자가 대화 온도와 질문 수를 골라 Room을 만든다.
2. 참가자는 QR을 스캔해 각자의 휴대폰에서 들어온다.
3. 모두가 A/B 중 하나를 고른다.
4. 진행자가 선택을 마감하면 전체 분포를 함께 공개한다.
5. 이유를 이야기한 뒤 다음 질문으로 넘어간다.

주요 범위:

- 기존 단일 기기 밸런스 모드는 그대로 유지
- Host 질문 시작·마감·다음 질문 제어
- 참가 인원과 응답 완료 인원 표시
- 공개 전에는 다른 사람의 선택 비율을 숨김
- 새로고침과 네트워크 재연결 후 현재 질문 복구
- 중간 참가, 무응답과 중복 응답 처리
- Room 종료 시 닉네임과 선택 기록 삭제
- 큰 화면에서 함께 보기 좋은 결과 표시

완료 기준:

- 최소 2명부터 목표 인원까지 같은 질문과 결과를 본다.
- 오래된 화면에서 다음 질문을 중복 실행할 수 없다.
- 참가자 한 명의 선택을 다른 참가자와 연결해 장기 보관하지 않는다.
- 실시간 모드 장애가 기존 혼자 하기 모드에 영향을 주지 않는다.

---

### v2.5.0 — 모임 한 흐름

각각 떨어져 있던 공동체 도구를 실제 모임 순서대로 이어서 쓸 수 있게 한다.

주요 범위:

- 조 편성 → 나눔 순서 → 기도 후원 같은 추천 코스
- 필요한 단계만 고르는 나만의 모임 순서
- 단계 사이에서 명단과 직전 결과 이어 쓰기
- 나눔·기도·쉬는 시간을 위한 타이머
- 멀리서도 보이는 진행자용 큰 화면
- 실수한 단계 되돌리기와 결과 재생성 확인
- 브라우저를 닫았다 돌아와도 현재 코스 복구
- 모임 종료 후 개인정보 없는 요약 이미지

기본은 local-first로 유지한다. 여러 기기 동기화가 꼭 필요한 단계만 기존 Room을 선택적으로 사용한다.

완료 기준:

- 리더가 명단을 반복 입력하지 않고 세 단계 이상의 모임을 진행한다.
- 새로고침 후에도 명단, 현재 단계와 결과가 안전하게 복구된다.
- 종료하거나 직접 지우면 저장된 명단과 진행 상태가 삭제된다.

---

### v2.6.0 — 언제 와도 새롭게

엔진을 다시 만들지 않고도 모임과 계절에 맞는 콘텐츠를 꾸준히 추가할 수 있는 구조를 만든다.

주요 범위:

- 밸런스 게임: 첫 만남, 수련회, 신앙, 우정 등 주제 팩
- 최애 월드컵: 계절 음식, 활동, 찬양과 공동체 주제 후보
- 익명 나눔: 새가족, 리더, 청년부와 깊은 나눔 질문 세트
- 팩별 직접 링크, 공유 이미지와 좋아요 집계
- 공개일과 종료일에 따른 NEW·시즌 표시
- 중복 질문, 누락 이미지, 잘못된 ID와 금지 문구 자동 검사
- 오래된 링크와 저장 상태의 하위 호환

콘텐츠를 많이 추가하는 것보다 “어떤 모임에서 왜 쓰는지”가 분명한 팩만 공개한다.

완료 기준:

- 코드 로직을 수정하지 않고 검증된 데이터 추가만으로 새 팩을 배포할 수 있다.
- 모든 팩의 직접 링크, 이미지와 통계 variant가 서로 섞이지 않는다.
- 시즌 종료 뒤에도 공유된 기존 링크가 안전한 기본 화면으로 연결된다.

---

### v2.7.0 — 말하기 어려운 마음도 안전하게

익명으로 만나는 우리를 재사용해 익명 질문함과 기도제목 모으기를 제공한다.

후보 기능:

- 진행자가 목적에 맞는 Room 템플릿 선택
- 참가자가 이름 공개 없이 질문이나 기도제목 제출
- 진행자가 하나씩 공개하고 완료 처리
- 부적절한 내용 숨김, 신고와 Room 즉시 종료
- 제출 전 저장·삭제 시점과 공개 범위 안내
- 원문을 통계, 추천 또는 AI 학습에 사용하지 않음
- Room 종료 시 제출 내용 즉시 삭제

출시 전 필요한 판단:

- 진행자에게 어느 정도의 숨김·건너뛰기 권한을 줄지
- 자해, 폭력과 학대처럼 즉시 도움이 필요한 표현을 어떻게 안내할지
- 교회 리더가 저장을 요구하더라도 기본 삭제 원칙을 유지할지

안전 정책과 실제 진행자 검토 없이 익명 자유 입력 기능을 먼저 공개하지 않는다.

---

### v2.8.0 — 운영이 보이는 온기

사용자 개인정보를 열람하지 않고도 서비스가 건강한지 확인하는 운영 도구를 만든다.

주요 범위:

- API readiness, DB 연결, SSE 연결 수와 배포 버전
- 현재 활성 Room 수, 생성·완료·취소·만료 집계
- 요청 실패율, 응답 시간 p95와 재연결 횟수
- 콘텐츠별 조회·참여·완료·공유와 variant별 좋아요
- 자동 배포 성공·실패와 rollback 이력
- disk, memory, container restart와 backup 최신 상태
- 문제가 있는 기능만 숨길 수 있는 최소 feature flag
- 공개 사용자 화면과 분리된 관리자 인증

운영 화면에서는 이름, 답변 원문, 개별 사용자의 전체 행동 경로와 원본 IP를 제공하지 않는다.

완료 기준:

- 장애 발생 여부와 영향을 받는 기능을 한 화면에서 판단할 수 있다.
- 관리자 권한이 없는 요청은 어떤 운영 데이터도 읽지 못한다.
- 운영 도구 장애가 공개 서비스와 Room 진행을 막지 않는다.

---

### v3.0.0 — 공동체가 머무는 온기

반복적으로 같은 사람들이 온기를 사용하고 “여러 리더가 함께 준비하고 싶다”는 요구가 충분히 확인될 때만 시작한다.

가능한 범위:

- 공동체 공간과 리더·공동 진행자 역할
- 동의한 구성원만 포함하는 공용 명단
- 자주 쓰는 질문 팩과 모임 코스 공유
- 예약 Room, 행사별 입장 링크와 공동 진행
- 휴대폰과 PC 사이의 설정 동기화
- 누가 무엇을 변경했는지 확인하는 감사 이력
- 공동체 탈퇴, 명단 삭제와 전체 데이터 내보내기

착수 조건:

- 서로 다른 실제 공동체에서 공용 명단과 공동 진행 요청이 반복된다.
- 한 기기 localStorage만으로 생기는 문제가 실제 모임을 자주 방해한다.
- 계정 복구, 탈퇴, 동의, 보호자와 관리자 권한 정책을 먼저 문서화한다.
- 홈서버 한 대가 감당할 수 없는 사용량과 가용성 요구가 확인된다.
- 관리형 hosting, backup, monitoring과 incident 대응 비용을 감당할 수 있다.

계정과 장기 보관은 편리함만 보고 추가하지 않는다. 사용자가 기대하는 삭제·이동·복구 권리까지 함께 제공할 수 있을 때 출시한다.

## 6. 모든 릴리스에 함께 적용하는 품질 기준

### 콘텐츠

- 질문·선택지·결과 ID의 중복과 누락 검사
- 한글 맞춤법, 신앙 표현과 대상 연령 검토
- 정답처럼 보이거나 특정 성향을 낮춰 보는 문구 방지
- 공개된 scoring 변경 시 content version 분리

### 화면과 접근성

- 320px 이상, safe area와 작은 화면 높이 대응
- 키보드만으로 핵심 흐름 완료
- 화면 읽기용 제목, label, 상태 안내
- prefers-reduced-motion 대응
- 로딩, 빈 상태, 오류와 재시도 화면 포함

### 브라우저와 공유

- Android Chrome, Samsung Internet, iOS Safari 확인
- 카카오톡 등 인앱 브라우저의 복사·저장 fallback
- 직접 링크와 browser back 동작
- Open Graph 제목·설명·이미지 실제 공유 검증
- PWA update와 offline cache 회귀 검사

### 데이터와 운영

- migration은 이전 운영 DB에서 앞으로만 적용
- idempotency, optimistic lock과 Room 권한 경계 테스트
- 이름·답변의 삭제 시점 검증
- backup과 restore 가능 여부 확인
- 배포 전 frontend·backend 전체 테스트
- readiness 실패 시 공개 전환 중단 또는 자동 rollback

## 7. 제품이 좋아지고 있는지 보는 기준

숫자는 사람을 식별하기 위해서가 아니라 어떤 흐름을 고쳐야 하는지 판단하기 위해 사용한다.

| 관점 | 확인할 지표 |
|---|---|
| 발견 | 콘텐츠 조회 대비 참여 시작 비율 |
| 몰입 | 참여 시작 대비 완료율과 중단 지점 |
| 나눔 | 완료 후 결과·링크 공유 비율 |
| 재방문 | 익명 visitor의 기간별 재방문 |
| 공동체 도구 | 도구별 시작·완료와 반복 사용 |
| Room | 생성 대비 정상 완료, 입장 성공률, 재연결 횟수 |
| 안정성 | 요청 실패율, 응답 p95, container restart와 rollback |

문항별 개인 답변, 한 사람의 결과 이력과 나눔 원문은 이 지표를 위해 추가 수집하지 않는다.

## 8. 당장 하지 않는 것

- 요구가 확인되지 않은 일반 회원가입과 소셜 로그인
- 광고를 위한 사용자 추적과 device fingerprint
- 익명 답변 원문을 오래 보관하는 관리자 검색
- 개인의 신앙이나 심리 상태를 단정하는 진단
- 안전 기준 없는 AI 답변 분석·요약
- 기능마다 별도 backend를 만드는 microservice 분리
- 필요가 확인되기 전의 Kafka, Redis cluster와 data warehouse
- 웹 경험이 충분히 검증되기 전의 별도 native app

## 9. 바로 다음 작업 순서

**v2.3.0 구르미 테스트**를 다음 중심 작업으로 둔다.

1. 여덟 캐릭터 정의와 결과 문구 확정
2. 질문·선택지·scoring·동점 정책 문서화
3. domain model과 scoring 자동 테스트
4. 인트로·질문·분석·결과 화면 구현
5. 여덟 결과 이미지와 공유 metadata 연결
6. 익명 참여·완료·결과 통계 연결
7. 실제 모바일과 인앱 브라우저 QA
8. 티저를 정식 진입 카드로 바꾸고 v2.3.0 배포
9. 출시 후 오류와 분포를 확인해 v2.3.x 안정화

진행 중 치명적 오류나 홈서버 운영 문제가 발견되면 v2.2.x patch를 먼저 배포하고 다시 구르미 작업으로 돌아온다.

## 10. 로드맵 운영 방식

- 한 번에 하나의 릴리스를 중심 작업으로 둔다.
- 새 아이디어는 바로 구현하지 않고 가장 가까운 후보 버전에 배치한다.
- 각 버전을 시작할 때 범위와 제외 항목을 다시 확인한다.
- 기능을 추가하면서 개인정보 보관 범위를 몰래 넓히지 않는다.
- 배포 시 [CHANGELOG](./CHANGELOG.md), 앱 업데이트 내역과 package version을 함께 갱신한다.
- 완료하지 못한 항목을 버전 번호만 올려 다음 릴리스로 숨기지 않는다.
- 실제 사용자 반응이 가정을 뒤집으면 버전 순서를 바꿀 수 있으며 그 이유를 이 문서에 남긴다.
